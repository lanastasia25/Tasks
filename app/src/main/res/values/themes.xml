<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Tasks" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Фон для темной темы -->
        <item name="android:windowBackground">@color/dark_background</item>

        <!-- Основные цвета Material3 -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/surface_color</item>
        <item name="colorOnSurface">@color/white</item>

        <!-- Глобальные стили текста -->
        <item name="android:textColorPrimary">@color/white</item> <!-- Основной текст -->
        <item name="android:textColorSecondary">@color/white</item> <!-- Второстепенный текст -->
    </style>

    <style name="Theme.Tasks" parent="Base.Theme.Tasks" />
</resources>